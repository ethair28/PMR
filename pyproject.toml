[project]
name = "pmr"
version = "0.1.0"
description = "Prediction-market repricing monitor for news-intelligence workflows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pmr = "pmr.cli:main"
